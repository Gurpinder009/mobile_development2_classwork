<resources>

    <!-- Base theme for the app -->
    <style name="Base.Theme.AndroidFinalProject" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/text_color_dark</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:windowBackground">@color/window_background_dark</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Toolbar style -->
    <style name="MyToolbarNavigation" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="colorControlNormal">@color/text_color_dark</item>
        <item name="titleTextColor">@color/text_color_dark</item>
    </style>

    <!-- Bottom Navigation style -->
    <style name="MyBottomNavigation" parent="ThemeOverlay.Material3.BottomAppBar">
        <item name="android:background">@color/primary_dark</item>
        <item name="backgroundTint">@color/primary_dark</item>
        <item name="itemIconTint">@color/light_black</item>
        <item name="itemTextColor">@color/light_black</item>
        <item name="itemActiveIndicatorStyle">@style/MyBottomNavigationActiveIndicator</item>
    </style>

    <!-- Active indicator for Bottom Navigation -->
    <style name="MyBottomNavigationActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/primary_dark</item>
    </style>

    <!-- Popup theme style -->
    <style name="MyPopupTheme" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:backgroundTint">@color/popup_background_dark</item>
    </style>

</resources>